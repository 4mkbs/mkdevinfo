// Root build.gradle now uses the plugins DSL only in module build files.
// No repositories or buildscript block here to comply with
// dependencyResolutionManagement { repositoriesMode = FAIL_ON_PROJECT_REPOS }.

task clean(type: Delete) {
    delete rootProject.buildDir
}
